server:
  port: 8090

spring:
  application:
    name: ai-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/kizuna-data

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/Kizuna